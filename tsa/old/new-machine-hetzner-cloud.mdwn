Go to the [Heztner console](https://console.hetzner.cloud/) and clikety on the web interface to get
a new instance. Credentials are in `tor-passwords.git` under `hetzner`.

Then follow [[new-machine]].
